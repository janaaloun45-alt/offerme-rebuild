# Copy to server/.env and fill in your own values. Never commit server/.env
MONGODB_URI=
JWT_SECRET=
# Comma-separated list of allowed frontend origins (also enables CORS for Lovable/Vercel/localhost automatically)
FRONTEND_URL=http://localhost:8080,https://offerme-rebuild-hqvw.vercel.app
PORT=5000
